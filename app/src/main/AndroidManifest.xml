<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".signup"></activity>
        <activity android:name=".Timeline" />
        <activity android:name=".drbot" />
        <activity android:name=".medstats" />
        <activity android:name=".symptommanagement" />
        <activity android:name=".home" />
        <activity android:name=".bookdr" />
        <activity android:name=".personaldetailsactivity" />
        <activity android:name=".MainActivity2signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>